export type LinkSet = {
  text: string;
  link: string;
};
export type ApproachesType = {
  title: string;
  description: string;
};
export type ProjectsType = {
  title: string;
  link: string;
  thumbnail: string;
};
